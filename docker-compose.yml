services:
  webapp:
    build:
      context: ./app
    ports:
      - "5000:5000"

  prometheus:
    image: prom/prometheus:latest
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "9090:9090"

# NOTE: cadvisor is disabled by default in this compose file because it often fails
# on Windows/Docker Desktop (requires host cgroups mounts). To enable on a Linux
# host, uncomment the block below and ensure the service runs with the required
# volume mounts and privileges.
#
#  cadvisor:
#    image: google/cadvisor:latest
#    ports:
#      - "8080:8080"
#    volumes:
#      - /:/rootfs:ro
#      - /var/run:/var/run:ro
#      - /sys:/sys:ro
#      - /var/lib/docker/:/var/lib/docker:ro
#    # Uncomment the following if your host requires privileged mode
#    # privileged: true

